<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
    <link rel="stylesheet" href="/style.css">
    <style>
        .NavbarItems {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 0 30px;
            box-shadow: 0 5px 15px rgb(0, 0, 0);
            height: 80px;
            border-radius: 15px;
        }

        .navbar-logo {
            color: black;
            font-size: 2;
        }

        .nav-menu {
            display: grid;
            grid-template-columns: repeat(6, auto);
            grid-gap: 15px;
            list-style: none;
            align-items: center;
        }

        .nav-links {
            text-decoration: none;
            color: black;
            font-size: 1.2rem;
            padding: 0.7rem 1rem;
            white-space: nowrap;
        }

        .nav-links i {
            padding-right: 10px;
        }

        .nav-links:hover {
            background-color: green;
            color: white;
            border-radius: 5px;
            transition: 0.2s linear;
        }

        .nav-links-mobile {
            display: none;
        }

        button {
            padding: 0.5rem 1rem;
            white-space: nowrap;
            border-radius: 0.3rem;
            border: none;
            cursor: pointer;
        }

        button:hover {
            background-color: black;
            color: white;
            transition: 0.2s linear;
        }

        .container {
            display: flex;
            flex-wrap: wrap;
            gap: 16px;
            padding: 20px;
        }

        .card {
            width: 18rem;
            border: 1px solid #ddd;
            border-radius: 10px;
            overflow: hidden;
            box-shadow: 0 2px 6px rgba(0, 0, 0, 0.15);
            transition: transform 0.2s;
        }

        .card:hover {
            transform: scale(1.03);
        }

        .card-img {
            width: 100%;
            height: 180px;
            object-fit: cover;
        }

        .card-body {
            padding: 1rem;
        }

        .card-title {
            font-size: 1.2rem;
            font-weight: bold;
            margin: 0 0 0.5rem;
        }

        .card-text {
            font-size: 0.95rem;
            color: #555;
            margin-bottom: 1rem;
        }

        .card-btn {
            display: inline-block;
            text-decoration: none;
            background: #007bff;
            color: #fff;
            padding: 0.5rem 1rem;
            border-radius: 6px;
            transition: background 0.2s;
        }

        .card-btn:hover {
            background: #0056b3;
        }
    </style>
</head>

<body>
    <nav class="NavbarItems">
        <h1 class="navbar-logo">Title</h1>

        <ul class="nav-menu">
            <li><a class="nav-links" href="/">Home</a></li>
            <li><a class="nav-links" href="/addNovel">เพิ่มนิยาย</a></li>
            <li><a class="nav-links" href="/login">Login</a></li>
            <li><a class="nav-links" href="/register">Register</a></li>

        </ul>
    </nav>
    <div class="container mt-4">
  <div class="row">
    <% if (data.length > 0) { %>
      <% data.forEach(novel => { %>
        <div class="col-md-4 mb-3">
          <div class="card">
            <img src="<%= novel.url %>" class="card-img-top" alt="<%= novel.name %>">
            <div class="card-body">
              <h5 class="card-title"><%= novel.name %></h5>
              <p class="card-text"><%= novel.text %>...</p>
              <a href="/read/<%= novel.idNovel1 %>" class="btn btn-primary">Read</a>
            </div>
          </div>
        </div>
      <% }) %>
    <% } else { %>
      <p>No novels found.</p>
    <% } %>
  </div>
</div>
    </div>
    

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM"
        crossorigin="anonymous"></script>
</body>

</html>