<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>
        <%= novel.title %>
    </title>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet">
</head>

<body>
    <header>
        <div class="px-3 py-2 bg-dark text-white">
            <div class="container">
                <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
                    <a href="/index"
                        class="d-flex align-items-center my-2 my-lg-0 me-lg-auto text-white text-decoration-none">
                        <i class="fa-solid fa-book-quran fa-4x"></i>
                        <h3 class="ms-2">Novel Website</h3>
                    </a>

                    <ul class="nav col-12 col-lg-auto my-2 justify-content-center my-md-0 text-small">
                        <li><a href="/index" class="nav-link text-secondary"><i
                                    class="fa-solid fa-house d-block mx-auto mb-1 fa-2x"></i>Home</a></li>
                        <li><a href="/addNovel" class="nav-link text-white"><i
                                    class="fa-solid fa-pen-nib d-block mx-auto mb-1 fa-2x"></i>Create Novel</a></li>
                        <li><a href="/coin" class="nav-link text-white"><i
                                    class="fa-solid fa-coins d-block mx-auto mb-1 fa-2x"></i>Coins</a></li>
                        <li><a href="/profile" class="nav-link text-white"><i
                                    class="fa-solid fa-user d-block mx-auto mb-1 fa-2x"></i>Profile</a></li>
                    </ul>
                </div>
            </div>
        </div>

        <div class="px-3 py-2 border-bottom mb-3">
            <div class="container d-flex flex-wrap justify-content-center">
                <form class="col-12 col-lg-auto mb-2 mb-lg-0 me-lg-auto">
                    <input type="search" class="form-control" placeholder="Search..." aria-label="Search">
                </form>

                <div class="text-end">
                    <% if (loggedIn) { %>
                        <a href="/logout" class="btn btn-danger">
                            <i class="fa-solid fa-right-from-bracket"></i> Logout
                        </a>
                        <% } else { %>
                            <a href="/login" class="btn btn-light text-dark me-2">Login</a>
                            <a href="/register" class="btn btn-primary">Sign-up</a>
                            <% } %>
                </div>
            </div>
        </div>
    </header>
    <div class="container mt-5">
        <h1 class="mb-4">
            <%= novel.title %>
        </h1>

        <div class="mb-4">
            <h3>Chapters</h3>
            <% if (chapters && chapters.length> 0) { %>
                <ul class="list-group">
                    <% chapters.forEach(chap=> { %>
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            Chapter <strong>
                                <%= chap.chapter_number %>
                            </strong>

                            <% if (chap.locked) { %>
                         
                                <form method="POST" action="/chapter/<%= chap.chapter_id %>/unlock" class="m-0">
                                    <button type="submit" class="btn btn-warning btn-sm">
                                        Unlock (<%= chap.cost %> coins)
                                    </button>
                                </form>
                                <% } else { %>
                           
                                    <a href="/chapter/<%= chap.chapter_id %>" class="btn btn-primary btn-sm">Read</a>
                                    <% } %>
                        </li>
                        <% }) %>
                </ul>
                <% } else { %>
                    <p class="text-muted">No chapters yet.</p>
                    <% } %>
        </div>

        <a href="/novel/<%= novel.novel_id %>/addChapter" class="btn btn-success">Add Chapter</a>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>